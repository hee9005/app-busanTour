<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="messages">
	<insert id="createmessage" parameterType="hashmap">
		insert into
		guest_book(board_id, users_id, content, writer, writed)
		values(boards_SEQ.NEXTVAL, #{userId}, #{content}, #{writer}, SYSDATE)
	</insert>
	<insert id="createAnonymous" parameterType="hashmap">
		insert into
		guest_book(board_id, board_pass, content, writer, writed)
		values(boards_SEQ.NEXTVAL, #{boardPass}, #{content}, #{writer},
		SYSDATE)
	</insert>
	<delete id="deleteBoard">
		delete from guest_book where board_id = #{boardId}
	</delete>
</mapper> 
 